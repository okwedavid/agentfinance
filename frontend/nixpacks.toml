[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --include=dev", "npm install --force @next/swc-linux-x64-musl"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node .next/standalone/server.js"